# Specialist Systems Component

Specialist systems remain authoritative for the live structured fields they are selected to own.

Examples:

- CRM -> contacts, deals and pipeline fields;
- accounting -> invoices, payments and ledgers;
- ATS -> candidate/application stages;
- service platform -> production/service state.

GitHub can own the work/decision around those systems without duplicating their whole database.
